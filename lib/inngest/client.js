// client.js
import { Inngest } from "inngest";

export const inngest = new Inngest({
    id: "mock-mind",   // keep it lowercase + hyphenated (best practice)
    name: "MockMind",
});
// Use your app's ID